"""
KaspiPulse — конфигурация скрейпера.

Все настройки в одном месте. Параметры городов подтверждены через DevTools
на kaspi.kz (boevye запросы /yml/product-view/pl/results).
"""

# ---------------------------------------------------------------------------
# НИША ДЛЯ СБОРА
# ---------------------------------------------------------------------------
# Категория задаётся внутри q в формате Kaspi:
#   :category:<КОД>:availableInZones:<ЗОНА>
#
# Чтобы сузить нишу до подкатегории — открой её на kaspi.kz и через DevTools
# скопируй код из q. Пока стартуем с широкой "Home" (Товары для дома и дачи).
CATEGORY_CODE = "Home"
NICHE_NAME = "Товары для дома"
NICHE_SLUG = "home"

# ---------------------------------------------------------------------------
# ГОРОД И ЗОНА ДОСТАВКИ
# ---------------------------------------------------------------------------
# Kaspi использует ДВЕ разные системы зон:
#   - для городов с Magnum-присутствием: именованные Magnum_ZONE*
#   - для остальных: зона = просто city_id
# Параметры ниже подтверждены через DevTools на живых запросах Kaspi.
CITIES = {
    "astana":   {"city_id": "710000000", "zone": "Magnum_ZONE5"},  # подтверждено
    "almaty":   {"city_id": "750000000", "zone": "Magnum_ZONE1"},  # подтверждено
    "shymkent": {"city_id": "511010000", "zone": "511010000"},     # подтверждено
}
ACTIVE_CITY = "astana"   # << меняй здесь, остальное подтянется автоматически

CITY_ID        = CITIES[ACTIVE_CITY]["city_id"]
AVAILABLE_ZONE = CITIES[ACTIVE_CITY]["zone"]

# ---------------------------------------------------------------------------
# ПАРАМЕТРЫ СБОРА
# ---------------------------------------------------------------------------
SORT = "relevance"
MAX_PAGES = 30        # ~12 товаров на страницу
PAGE_SIZE = 12

# ---------------------------------------------------------------------------
# ВЕЖЛИВОСТЬ (этичный скрейпинг)
# ---------------------------------------------------------------------------
REQUEST_DELAY_SEC = (2.0, 5.0)
MAX_RETRIES = 3
RETRY_BACKOFF_SEC = 5
TIMEOUT_SEC = 20

USER_AGENT = (
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
    "AppleWebKit/537.36 (KHTML, like Gecko) Chrome/124.0 Safari/537.36"
)

# ---------------------------------------------------------------------------
# МЕТОДОЛОГИЯ ОЦЕНКИ ПРОДАЖ (PROJECT_PLAN.md, раздел 2)
# ---------------------------------------------------------------------------
# Доля покупателей, оставляющих отзыв.
# 2% — стартовое значение по аналогии с Amazon.
# Для Казахстана реальная цифра почти наверняка ВЫШЕ — Kaspi платит бонусы за
# отзывы, что стимулирует пользователей писать чаще. Калибровка коэффициента —
# отдельная аналитическая задача на накопленных данных.
REVIEW_RATE = 0.02

# ---------------------------------------------------------------------------
# ПУТИ
# ---------------------------------------------------------------------------
RAW_DIR = "data/raw"
WEEKLY_DIR = "data/weekly"
